This is a sample app for job interveiw
it collect data from user validate that works for the database, 
and inserts it into a postgresql database with tables created with the SQL scripts included in SQL folder 